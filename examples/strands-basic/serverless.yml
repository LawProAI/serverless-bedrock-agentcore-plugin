service: strands-basic-agent

frameworkVersion: '4'

provider:
  name: aws
  region: ${opt:region, 'us-east-1'}
  stage: ${opt:stage, 'dev'}

plugins:
  - ../../ # Local plugin reference (use 'serverless-bedrock-agentcore' when installed from npm)

custom:
  agentCore:
    defaultTags:
      Project: ${self:service}
      Framework: Strands
      ManagedBy: Serverless

agents:
  # AgentCore Runtime - Strands agent deployment
  strandsAgent:
    type: runtime
    description: Basic Strands AI agent with custom tools
    artifact:
      docker:
        path: .
        file: Dockerfile
        repository: strands-basic-agent
    protocol: HTTP
    network:
      networkMode: PUBLIC
