[project]
name = "full-stack-agent"
version = "0.1.0"
description = "Full-stack AI agent with browser and code execution"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
    "boto3>=1.35.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]
