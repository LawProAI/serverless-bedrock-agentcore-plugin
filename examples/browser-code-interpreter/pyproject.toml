[project]
name = "browser-code-interpreter"
version = "0.1.0"
description = "Research agent with browser and code execution"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
    "boto3>=1.35.0",
]
